import 'angular-mocks/angular-mocks';

// fixme: write tests one api is frozen

import dtrwTranslate from '.';

describe(dtrwTranslate.name, () => {

  beforeEach(function () {
    window.module(dtrwTranslate.name);

    inject($injector => {
    });
  });

});
